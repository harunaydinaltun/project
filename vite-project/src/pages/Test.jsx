import SearchSideBar from "../components/SearchSideBar";

export const Test = ({ t }) => {
  return (
    <div>
      TEST
      <SearchSideBar t={t}></SearchSideBar>
    </div>
  );
};

export default Test;
